pipeline {
    agent any

    stages {
         stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('PR Validation') {
            steps {
                echo 'Deu baom!'
            }
        }
    }
}
